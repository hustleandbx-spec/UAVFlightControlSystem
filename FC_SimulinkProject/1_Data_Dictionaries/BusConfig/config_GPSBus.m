function cfg = config_GPSBus()
%CONFIG_GPSBUS 契约 #7：GPS/RTK 测量帧（含有效性与采样时间戳）。
% 供 StateEstimation 消费（GPS 中速直连，DEC-072/074；RTK 定位+定向测量输入，DEC-105）。

cfg.busName = 'GPSBus';
cfg.description = 'GPS/RTK 测量帧（含有效性与采样时间戳）';
cfg.elements = { ...
    'Lat',         'single',  1, 'deg', '纬度'; ...
    'Lon',         'single',  1, 'deg', '经度'; ...
    'Alt',         'single',  1, 'm',   '海拔'; ...
    'Velocity',    'single',  3, 'm/s', 'NED 速度'; ...
    'Valid',       'boolean', 1, '',    '测量帧有效标志'; ...
    'Timestamp',   'double',  1, 's',   '测量采样时间戳'; ...
    'IsNew',       'boolean', 1, '',    'GPS 消息为新帧'; ...
    };
end
